<%= form_with(model: restaurant_pizza) do |form| %>
  <% if restaurant_pizza.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(restaurant_pizza.errors.count, "error") %> prohibited this restaurant_pizza from being saved:</h2>

      <ul>
        <% restaurant_pizza.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :price, style: "display: block" %>
    <%= form.text_field :price %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
